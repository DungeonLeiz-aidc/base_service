"""
gRPC Interceptors Package

This package contains gRPC interceptors (middlewares) for logging, authentication, and error handling.
"""

from loguru import logger

logger.info("AUDIT | GRPC | Interceptors initialized")
